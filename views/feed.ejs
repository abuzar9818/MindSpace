<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore Posts | MindSpace</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">

        <!-- NAVBAR -->
        <nav class="w-full bg-zinc-800 border-b border-zinc-700 px-10 py-4">
            <div class="flex justify-between items-center">
                <h1 class="text-2xl font-bold text-blue-500">MindSpace</h1>

                <div class="flex gap-3 items-center">
                    <a href="/profile"
                        class="text-sm hover:text-gray-300 bg-gray-500 rounded-md px-4 py-2 inline-block transition-colors">
                        My Profile
                    </a>
                    <a href="/logout"
                        class="text-sm hover:text-gray-300 bg-red-500 rounded-md px-4 py-2 inline-block transition-colors">
                        Logout
                    </a>
                </div>
            </div>
        </nav>

        <!-- CONTENT -->
        <div class="p-10">
            <h3 class="text-3xl mb-2 font-semibold">🌍 Explore Posts</h3>
            <p class="text-gray-400 mb-8">See what others are sharing</p>

            <div class="grid grid-cols-3 gap-5">
                <% posts.reverse().forEach(function(post){ %>

                    <div
                        class="border border-zinc-600 rounded-md p-4 bg-zinc-800 hover:bg-black transition-all duration-300 ease-out hover:scale-105 hover:shadow-xl">

                        <!-- USER NAME -->
                        <h4 class="text-blue-500 mb-2">
                            <%= post.user ? post.user.username : "Unknown User" %>
                        </h4>

                        <!-- POST CONTENT -->
                        <p class="text-md tracking-tight">
                            <%= post.content %>
                        </p>

                        <!-- LIKES -->
                        <small class="mt-2 inline-block text-gray-400">
                            <%= post.likes.length %> likes
                        </small>

                        <!-- ACTIONS -->
                        <div class="flex gap-4 pt-4">
                            <a class="text-blue-400 hover:text-blue-300 font-medium transition-colors duration-200 flex items-center gap-1"
                               href="/like/<%= post._id %>?from=feed">
                                <%= post.likes.indexOf(loggedInUserId) === -1 ? '❤️ Like' : '💔 Unlike' %>
                            </a>
                        </div>
                    </div>

                <% }) %>
            </div>
        </div>

        <!-- FOOTER -->
        <footer class="w-full bg-zinc-800 border-t border-zinc-700 mt-10">
            <div class="px-10 py-8">
                <div class="grid grid-cols-3 gap-8 mb-6">
                    <div>
                        <h4 class="text-white font-bold text-lg mb-3">Notes</h4>
                        <p class="text-zinc-400 text-sm">
                            Discover thoughts and ideas shared by the community.
                        </p>
                    </div>
                    <div>
                        <h4 class="text-white font-bold text-lg mb-3">Quick Links</h4>
                        <ul class="space-y-2 text-zinc-400 text-sm">
                            <li><a href="/profile" class="hover:text-blue-500 transition-colors">Profile</a></li>
                            <li><a href="/feed" class="hover:text-blue-500 transition-colors">Explore</a></li>
                        </ul>
                    </div>
                    <div>
                        <h4 class="text-white font-bold text-lg mb-3">Connect</h4>
                        <ul class="space-y-2 text-zinc-400 text-sm">
                            <li><a href="#" class="hover:text-blue-500 transition-colors">Twitter</a></li>
                            <li><a href="#" class="hover:text-blue-500 transition-colors">GitHub</a></li>
                            <li><a href="#" class="hover:text-blue-500 transition-colors">LinkedIn</a></li>
                        </ul>
                    </div>
                </div>

                <div class="border-t border-zinc-700 pt-6 text-center">
                    <p class="text-zinc-400 text-sm">© 2026 Notes. All rights reserved.</p>
                </div>
            </div>
        </footer>

    </div>
</body>

</html>
