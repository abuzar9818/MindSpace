<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center">
        
        <div class="w-full max-w-md bg-zinc-800 p-8 rounded-xl shadow-lg">
            <h3 class="text-3xl mb-6 text-center font-semibold">Create Account</h3>

            <form class="flex flex-col gap-4" action="/register" method="POST">
                <input class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500" type="text" placeholder="name" name="name">
                
                <input class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500" type="text" placeholder="username" name="username">
                
                <input class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500" type="password" placeholder="password" name="password">
                
                <input class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500" type="text" placeholder="email" name="email">
                
                <input class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500" type="number" placeholder="age" name="age">
                
                <input class="mt-4 px-3 py-2 bg-blue-500 hover:bg-blue-600 transition rounded-md cursor-pointer font-medium" type="submit" value="Create Account">
            </form>
           <p class="text-center text-sm text-zinc-400 mt-4">
                Already have an account?
                <a href="/login" class="text-blue-400 hover:underline">Login</a>
            </p>
        </div>

    </div>
</body>
</html>
