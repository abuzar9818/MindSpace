<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center">
        <div class="w-full max-w-md bg-zinc-800 p-8 rounded-xl shadow-lg">

            <h3 class="text-3xl mb-6 text-center font-semibold">
                Login Your Account
            </h3>

            <form class="flex flex-col gap-4" action="/login" method="POST">

                <input
                    class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500"
                    type="email" placeholder="Email" name="email" required>

                <input
                    class="px-3 py-2 outline-none bg-transparent border border-zinc-700 rounded-md focus:border-blue-500"
                    type="password" placeholder="Password" name="password" required>

                <input
                    class="mt-4 px-3 py-2 bg-blue-500 hover:bg-blue-600 transition rounded-md cursor-pointer font-medium"
                    type="submit" value="Login">

            </form>
            <p class="text-center text-sm text-zinc-400 mt-4">
                Don’t have an account?
                <a href="/" class="text-blue-400 hover:underline">Register</a>
            </p>

        </div>
    </div>

</body>

</html>